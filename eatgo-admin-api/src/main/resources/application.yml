server:
  port: 9091

spring:
  datasource:
    url: jdbc:h2:~/data/eatgo
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update

---

spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:test